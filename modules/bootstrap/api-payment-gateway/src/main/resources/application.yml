spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banana
    username: banana
    password: banana1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        jdbc.time_zone: UTC
    show-sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false
server:
  port: 8080

